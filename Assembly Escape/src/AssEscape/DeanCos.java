package AssEscape;

public class DeanCos extends Enemy{
	
	//he just extends enemy lol. but he's a unique type who will hopefully have circular vision so
	//made him his own class
	
	public DeanCos(int tileplacex, int tileplacey, int tilewidth, int move) {
		super(tileplacex, tileplacey, tilewidth, move, "img/DeanCos.png", 4);
	}

}
